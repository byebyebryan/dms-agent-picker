[project]
name = "dms-agent-picker"
version = "0.3.1"
description = "DMS launcher for local and SSH Codex and Claude Code sessions"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "Bryan Bai" }]

[tool.ruff]
line-length = 100
