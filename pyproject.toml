[project]
name = "dms-agent-picker"
version = "0.1.0"
description = "DMS launcher for local and SSH Codex CLI sessions"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "Bryan Bai" }]

[tool.ruff]
line-length = 100
